language: python
python:
  - 2.6
  - 2.7
  - 3.3
env:
  - DJANGO=Django==1.4.10
  - DJANGO=Django==1.5.5
  - DJANGO=Django==1.6.1
install:
  - pip install $DJANGO
  - python setup.py install
  - pip install -r testproject/requirements_travis.txt
script:
  - cd testproject
  - python manage.py test
  - coverage run --source=django_docopt_command,testproject manage.py test
  - coverage report -m
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq sloccount
after_success:
  - coveralls
  - cd ..
  - covio
matrix:
  exclude:
    - python: 3.3
      env: DJANGO=Django==1.4.10